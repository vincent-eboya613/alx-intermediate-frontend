// Define RowID as a number
export type RowID = number;

// Define RowElement interface with optional age property
export interface RowElement {
    firstName: string;
    lastName: string;
    age?: number;
}
